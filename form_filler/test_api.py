import requests

try:
    # Need to pass the admin auth token. Let's just mock the request or bypass it.
    # Actually, simpler to just read what the API says. Let's look at the database.
    pass
except Exception as e:
    pass
